export interface SimulationConfig {
    deviceId: string;
    idScope: string;
    deviceKey: string;
    gatewayName: string;
}

export interface DeviceConfig {
    deviceId: string;
    idScope: string;
    deviceKey: string;
    gatewayName: string;
}